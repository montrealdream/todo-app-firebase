/**
 * Hàm chuẩn hóa tên của người, content
 * Viết hoa chữ Cái đầu tiên của mỗi từ
 * @param {*} str 
 */
export const chuanHoaTen = (str) => {
    let arrWord = str.split(" ");

    console.log(arrWord);
}